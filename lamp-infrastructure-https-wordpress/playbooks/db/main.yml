---
- hosts: lamp_db
  become: yes

  vars_files:
    - vars.yml

  pre_tasks:
    - name: Create dynamic MySQL variables.
      set_fact:
        mysql_users:
          - name: admin
            host: "{{ groups['lamp_www'][0] }}"
            password: secretbiengarde6490
            priv: "*.*:SELECT"
          - name: admin
            host: "{{ groups['lamp_www'][1] }}"
            password: secretbiengarde6490
            priv: "*.*:SELECT"
        mysql_replication_master: "{{ groups['a4d.lamp.db.1'][0] }}"

  roles:
    - geerlingguy.firewall
    - geerlingguy.mysql
